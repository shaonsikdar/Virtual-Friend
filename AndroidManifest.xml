<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.virtualfriend"
      android:versionCode="1"
      android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-sdk android:minSdkVersion = "15"/>
 	<uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.READ_OWNER_DATA"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application android:icon="@drawable/ic_launcher" android:label="@string/app_name">
   
        <activity android:name="loginActivity"
                  android:label="Login Your Account">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
            <!--  Entry for registrationActivity.class -->
        <activity android:name="registrationActivity"
                  android:label="Register New Account">
        </activity>
        
        <!--  Entry for chatbotActivity.class -->
        <activity android:name="chatbotActivity"
                  android:label="Chat With Virtual Friend">
        </activity>
        
       
    </application>
    
</manifest>